swap: 4096 MB
Root: 25000 MB
Home: 30904 MB

sudo apt update && sudo apt upgrade -y && sudo apt install vim vim-gtk git encfs inkscape xbindkeys xdotool gthumb xsel -y && set -o vi

sudo add-apt-repository ppa:unit193/encryption && sudo apt update && sudo apt install veracrypt


theme
appearance
mouse
notifications
change workspace hotkeys

set grub default to last used:
sudo vim /etc/default/grub
GRUB_DEFAULT=saved
GRUB_SAVEDEFAULT=true

sudo update-grub
